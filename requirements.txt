# Core dependencies
httpx>=0.25.0
tenacity>=8.2.0
python-dotenv>=1.0.0

# Development dependencies (install with: pip install -r requirements.txt -r requirements-dev.txt)
# Or install with: pip install -e ".[dev]"
